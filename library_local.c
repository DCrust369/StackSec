#include <stdio.h>

continue asm () void {
    /*
    * local library
    * this library
    * it's for my app
    * it's for allocation of the
    * codes
    */
    const name(): i32 = (johnny_the_slack());
    void process_acess.txt() { printf("Processing acess.txt now ok!\n");}
    void process_acess.txt() { printf("others process\n");}
    const file_management.c(): i32 = [cd.sh()];
    void acess_go() { printf("go to bash\n")}
}