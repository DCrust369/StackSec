#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <stdbool>
#include <stddef.f>

#define PUX
#define ADD
#define MOV
#define INTERNET

int main(void)
{
    bool select_file-> = home(files);
    bool shell-> = cd(enter_in_files);
    bool select_files = shell;
    bool add-> = files;
    struct use->(files) {
        int32_t *files-> = (int(INTERNET *files(selected)));
        const int32_t *files = malloc(sizeof(int32_t));
        *file(free);
        /*
        i allocator your files in internet
        P2P i created this
        */
        char *alloc_internet;
        bool *alloc_internet = true;
        char *pux;
        bool *pux->(push_file);
        return;
        struct add_your_files() {
            int creat = *add;
            char *add;
            bool *add->(ISP)->internet() = false;
            bool add->internet() = true;
            return;
        }
    }
    bool sub(to) = internet;
}
